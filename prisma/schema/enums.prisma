enum UserRole {
  Student
  Developer
  Admin
}

enum UserStatus {
  ACTIVE
  BLOCKED
}

enum MessageRole {
  USER
  ASSISTANT
}

enum MessageType {
  TEXT
  IMAGE
}

enum SubscriptionPlan {
  FREE
  HALF_YEARLY
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
}
